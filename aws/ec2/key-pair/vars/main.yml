---
module_vars:
  path: aws
  ns: ec2
  name: key-pair

variables:
  key_name:
    description: The name of the key pair to create
  public_key_path:
    description: The name of the key pair to create

remote_state_vars: {}

output_vars: {}
