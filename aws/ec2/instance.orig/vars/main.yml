---
module_meta:
  ns: ec2
  name: instance
  path: aws/ec2/instance

variables:
  component:
    description: 'description'
  environment:
    description: 'description'
  key_name:
    description: 'description'
  instance_type:
    description: 'The size of the instance'
    default: t2.medium
  region:
    description: 'description'
  resource:
    description: 'description'
  service:
    description: 'description'

remote_state_vars:
  subnet_id:
    description: 'description'
    reference: 'vpc.subnet_id_public_1'
  vpc_security_group_ids:
    description: 'description'
    reference: 'vpc.security_group_id_public'
    #   vpc_id:
    #     description: 'The id of the VPC into which the instance will be launched'
    #     reference: 'vpc'

output_vars:
  instance_id:
    description: description
    value: aws_instance.app.instance_id
