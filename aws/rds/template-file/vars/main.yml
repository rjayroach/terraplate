---
resource_meta:
  path: aws
  ns: rds
  name: template-file

variables:
  key:
    description: The key under which the endpoint is written
  name:
    description: The name of the file into which the endpoint is written

# Needs to be set using:
#   export TF_VAR_rds_template_file_path=
env_vars:
  path:
    description: The path into which the file is written

external_vars:
  instance_address:
    description: description

output_vars: {}
