---
module_vars:
  path: aws
  ns: rds
  name: template-file

variables:
  key:
    description: The key under which to write the endpoint
  name:
    description: The name of the file which to write the endpoint to

# Needs to be set using:
#   export TF_VAR_rds_template_file_path=
env_vars:
  path:
    description: The path into which to write the file

external_vars:
  db_instance_address:
    description: description

output_vars: {}
