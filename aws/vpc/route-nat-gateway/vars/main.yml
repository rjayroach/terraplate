---
resource_meta:
  path: aws
  ns: vpc
  name: route-nat-gateway

variables:
  destination_cidr_block:
    description: description
    default: 0.0.0.0/0

external_vars:
  route_table_id:
    description: description
  nat_gateway_id:
    description: description

output_vars: {}
