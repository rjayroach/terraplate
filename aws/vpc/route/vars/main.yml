---
module_vars:
  path: aws
  ns: vpc
  name: route

variables:
  destination_cidr_block:
    description: description
    default: 0.0.0.0/0

remote_state_vars:
  main_route_table_id:
    description: description
    reference: vpc
  igw_id:
    description: description
    reference: igw
