---
resource_meta:
  path: aws
  ns: vpc
  name: route

variables:
  destination_cidr_block:
    description: description
    default: 0.0.0.0/0

external_vars:
  route_table_id:
    description: description
  gateway_id:
    description: description
