---
resource_meta:
  path: aws
  ns: vpc
  name: route-table-association

external_vars:
  route_table_id:
    description: The id of the route table
  subnet_id:
    description: The id of the subnet to attach to the route table
